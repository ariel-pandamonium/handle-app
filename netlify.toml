# Handle. — Netlify configuration
[build]
  command = "npm install && npm run build"
  publish = "dist"

# Single-page app: redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
